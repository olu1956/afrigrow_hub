-- Training portal v1 one-shot setup (safe to re-run)
-- Run in Supabase → SQL Editor.
-- Full schema: supabase/migrations/20260708150000_create_training_portal.sql

-- Paste and run the migration file contents, or apply via Supabase CLI:
--   supabase db push

select 'Run supabase/migrations/20260708150000_create_training_portal.sql in SQL Editor' as setup_hint;
